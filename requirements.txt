# Storypark Scraper – Python dependencies
# Install with: pip install -r requirements.txt

# Web scraping
playwright>=1.44.0

# Pre-compiled dlib for Windows (no CMake/MSVC needed).
# Falls back to source build on Linux/macOS where compilers are readily available.
dlib-bin; sys_platform == "win32"

# Facial recognition
face_recognition>=1.3.0
numpy>=1.24.0

# Image processing & EXIF manipulation
Pillow>=10.0.0
piexif>=1.1.3

# Google Photos upload
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
requests>=2.31.0

# Secure credential storage (stores Storypark password in the OS keychain)
keyring>=25.0.0
